# Example environment variables for backend
# SQLite database path (optional). If unset, defaults to career_platform_backend/data_preview.sqlite
SQLITE_DB=/data/data_preview.sqlite

# Admin shared secret for /admin/ingest-json endpoint
ADMIN_SHARED_SECRET=change-me-please

# Optional custom data root override
DATA_PREVIEW_ROOT=/app/kavia-docs/data
